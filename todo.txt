faire les events

il reste coté affichage
- calculer la distance et la taille du mur pour chaque colonne de la fenetre (une boucle sur le calcul de distance avec l'angle qui varie de -40 a +40) 
- selectioner le bon pixel de la bonne texture 
- on modifie l'image avec ce pixel
Coté mlx :
- importer les texture sous format image (mlxcreateImagefrom...()) et remplir notre structure correspondante
- creer les évènements de deplacement et de rotation

attention si angle devenait négatif?
vérifier si le joueur est dans un mur

moins d appel a calcule dist pour les events